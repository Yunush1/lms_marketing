// Side-effect CSS imports (e.g. import './globals.css') — Next.js handles
// the actual loading via its own webpack/turbopack config; we just need a
// module declaration so TypeScript doesn't complain.
declare module '*.css';
declare module '*.scss';
